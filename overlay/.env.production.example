# TODO: Copy this file to overlay/.env.production and replace the placeholders.
# These are public URLs only — do NOT commit secrets here. Real secrets live
# in the tenant Function App's configuration (synced from Key Vault).
#
# See SentryIQ/docs/TENANT_CONTRACT.md (section: overlay/.env.production).

VITE_API_BASE_URL=https://REPLACE_ME-api.azurewebsites.net
VITE_SIGNALR_NEGOTIATE_URL=https://REPLACE_ME-api.azurewebsites.net/api/signalr-negotiate
VITE_TENANT_BRANDING_JSON=./overlay/branding.json
